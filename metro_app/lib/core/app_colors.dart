import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF202F4B);  // Dark Blue
  static const Color primaryLight = Color(0xFF3A4F7A);  // Slightly Lighter Blue
  
  // Background Colors
  static const Color background = Color(0xFFE3E3E3);  // Light Gray
  static const Color white = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);
  
  // Text Colors
  static const Color textPrimary = Color(0xFF000000);  // Black
  static const Color textSecondary = Color(0xFF767676);  // Gray
  
  // Input Colors
  static const Color inputBackground = Color(0xFFE3E3E3);
  
  // Accent Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFFF0000);  // Red from asterisk
  static const Color warning = Color(0xFFFFC107);
  
  // Bottom Navigation Colors
  static const Color bottomNavBackground = Color(0xFF202F4B);
  static const Color bottomNavText = Color(0xFFFFFFFF);
  
  // Special Colors
  static const Color shadowColor = Color.fromRGBO(255, 255, 255, 0.5);
}